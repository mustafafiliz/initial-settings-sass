{
  "scripts": {
    "start": "npm run open",
    "open": "concurrently \"http-server -a localhost -p 1234\" \"open http://localhost:1234/build\" \"sass --watch assets/sass:assets/css\""
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "http-server": "^0.12.3",
    "node-sass": "^5.0.0"
  },
  "dependencies": {
    "flexboxgrid-sass": "^8.0.5",
    "normalize.css": "^8.0.1"
  }
}
